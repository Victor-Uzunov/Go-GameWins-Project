type Player {
  name: String!
  wins: Int!
}

type Query {
  league: [Player!]!
  player(name: String!): Player
}

type Mutation {
  addPlayer(name: String!, wins: Int!): Player
  recordWin(name: String!): Player
}
